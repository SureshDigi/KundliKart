const basePath = '../assets/images/';

const LANGUAGE = require(`${basePath}language.png`);
const APP_LOGO = require(`${basePath}applogo.png`);
const FACEBOOK = require(`${basePath}facebook.png`);
const GOOGLE = require(`${basePath}google.png`);
const EMAIL = require(`${basePath}email.png`);
const KUNDLI = require(`${basePath}symbol.png`);
const MATCH = require(`${basePath}match.png`);
const HOROSCOPE = require(`${basePath}libra.png`);
const PREDICTION = require(`${basePath}note.png`);

export {
  LANGUAGE,
  APP_LOGO,
  FACEBOOK,
  GOOGLE,
  EMAIL,
  KUNDLI,
  MATCH,
  HOROSCOPE,
  PREDICTION,
};
